---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString("vi-VN", {
			year: "numeric",
			month: "long",
			day: "numeric",
		})
	}
</time>
